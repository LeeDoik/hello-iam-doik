---
import type { Locale } from "../i18n/locales";
import { t } from "../i18n/ui";

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
---

<p role="note" class="border-line text-ink-2 my-6 max-w-[65ch] border-l-2 pl-4 text-sm">{t(locale, "story.fallbackNotice")}</p>
